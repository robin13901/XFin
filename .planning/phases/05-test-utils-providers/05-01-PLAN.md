---
phase: 05-test-utils-providers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/constants/spacing_test.dart
  - test/utils/modal_helper_test.dart
  - test/utils/snappy_scroll_physics_test.dart
  - test/providers/database_provider_test.dart
autonomous: true
requirements: [R2.1, R2.2]

must_haves:
  truths:
    - "Every constant in Spacing class has a test verifying its value"
    - "Every SizedBox widget in Spacing has a test verifying its dimensions"
    - "showFormModal opens a bottom sheet with isScrollControlled true"
    - "showFormModal passes the formWidget and returns its result"
    - "DatabaseProvider singleton pattern returns same instance"
    - "DatabaseProvider.initialize sets db and notifies listeners"
    - "DatabaseProvider.replaceDatabase closes old db, sets new, notifies"
    - "SnappyPageScrollPhysics overrides produce expected values"
    - "SnappyPageScrollPhysics.applyTo returns correct type"
  artifacts:
    - path: "test/constants/spacing_test.dart"
      provides: "Unit tests for all Spacing constants and SizedBox widgets"
      min_lines: 40
    - path: "test/utils/modal_helper_test.dart"
      provides: "Widget tests for showFormModal function"
      min_lines: 30
    - path: "test/utils/snappy_scroll_physics_test.dart"
      provides: "Unit tests for SnappyPageScrollPhysics overrides"
      min_lines: 40
    - path: "test/providers/database_provider_test.dart"
      provides: "Unit tests for DatabaseProvider singleton and lifecycle"
      min_lines: 50
  key_links:
    - from: "test/constants/spacing_test.dart"
      to: "lib/constants/spacing.dart"
      via: "direct import"
      pattern: "import.*spacing"
    - from: "test/utils/modal_helper_test.dart"
      to: "lib/utils/modal_helper.dart"
      via: "widget test calling showFormModal"
      pattern: "showFormModal"
    - from: "test/providers/database_provider_test.dart"
      to: "lib/providers/database_provider.dart"
      via: "singleton instance access and lifecycle methods"
      pattern: "DatabaseProvider\\.instance"
---

<objective>
Add 100% statement coverage tests for the 4 remaining untested utility/provider/constant files: spacing.dart, modal_helper.dart, snappy_scroll_physics.dart, and database_provider.dart.

Purpose: Close all remaining test coverage gaps for utilities and providers, completing Phase 5.
Output: 4 new test files (~25 total tests), all passing with flutter test and flutter analyze clean.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-utils-providers/CONTEXT.md

@lib/constants/spacing.dart
@lib/utils/modal_helper.dart
@lib/utils/snappy_scroll_physics.dart
@lib/providers/database_provider.dart

@test/helpers/test_helpers.dart
@test/widgets/test_helpers.dart

<interfaces>
<!-- Source files under test — executor needs these contracts -->

From lib/constants/spacing.dart:
```dart
class Spacing {
  Spacing._();
  static const double tiny = 4.0;
  static const double small = 8.0;
  static const double medium = 16.0;
  static const double large = 24.0;
  static const double huge = 32.0;
  static const Widget vTiny = SizedBox(height: tiny);
  static const Widget vSmall = SizedBox(height: small);
  static const Widget vMedium = SizedBox(height: medium);
  static const Widget vLarge = SizedBox(height: large);
  static const Widget vHuge = SizedBox(height: huge);
  static const Widget hTiny = SizedBox(width: tiny);
  static const Widget hSmall = SizedBox(width: small);
  static const Widget hMedium = SizedBox(width: medium);
  static const Widget hLarge = SizedBox(width: large);
  static const Widget hHuge = SizedBox(width: huge);
}
```

From lib/utils/modal_helper.dart:
```dart
Future<T?> showFormModal<T>(BuildContext context, Widget formWidget)
// Wraps showModalBottomSheet with isScrollControlled: true
```

From lib/providers/database_provider.dart:
```dart
class DatabaseProvider extends ChangeNotifier {
  static DatabaseProvider _instance = DatabaseProvider._internal();
  static DatabaseProvider get instance => _instance;
  @visibleForTesting static set instance(DatabaseProvider provider) => _instance = provider;
  late AppDatabase _db;
  AppDatabase get db => _db;
  void initialize(AppDatabase db);           // sets _db, notifyListeners
  Future<void> replaceDatabase(AppDatabase newDb);  // closes old, sets new, notifyListeners
}
```

From lib/utils/snappy_scroll_physics.dart:
```dart
class SnappyPageScrollPhysics extends PageScrollPhysics {
  const SnappyPageScrollPhysics({super.parent});
  SnappyPageScrollPhysics applyTo(ScrollPhysics? ancestor);
  double get minFlingDistance => 1.0;
  double get minFlingVelocity => 15.0;
  double get maxFlingVelocity => 20000.0;
  double carriedMomentum(double existingVelocity);
  SpringDescription get spring;
}
```

<!-- Existing test patterns to follow -->

From test/providers/base_currency_provider_test.dart:
- Pure unit test with setUp/tearDown
- Uses addListener(() => notified = true) to verify notifyListeners

From test/providers/theme_provider_test.dart:
- Uses group() for logical test organization
- Tests singleton .instance access
- Verifies notifyListeners via addListener

From test/utils/format_test.dart:
- Simple group/test structure for pure functions
- Direct import and assertion

From test/helpers/test_helpers.dart:
- TestHelpers.createTestDatabase() for in-memory AppDatabase
- TestHelpers.initializeDatabaseProvider(db)
- DatabaseProvider.instance pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for spacing.dart and snappy_scroll_physics.dart</name>
  <files>test/constants/spacing_test.dart, test/utils/snappy_scroll_physics_test.dart</files>
  <behavior>
    spacing_test.dart (~15 tests):
    - Test 1: Spacing.tiny equals 4.0
    - Test 2: Spacing.small equals 8.0
    - Test 3: Spacing.medium equals 16.0
    - Test 4: Spacing.large equals 24.0
    - Test 5: Spacing.huge equals 32.0
    - Test 6: Spacing.vTiny is SizedBox with height 4.0
    - Test 7: Spacing.vSmall is SizedBox with height 8.0
    - Test 8: Spacing.vMedium is SizedBox with height 16.0
    - Test 9: Spacing.vLarge is SizedBox with height 24.0
    - Test 10: Spacing.vHuge is SizedBox with height 32.0
    - Test 11: Spacing.hTiny is SizedBox with width 4.0
    - Test 12: Spacing.hSmall is SizedBox with width 8.0
    - Test 13: Spacing.hMedium is SizedBox with width 16.0
    - Test 14: Spacing.hLarge is SizedBox with width 24.0
    - Test 15: Spacing.hHuge is SizedBox with width 32.0

    snappy_scroll_physics_test.dart (~8 tests):
    - Test 1: minFlingDistance is 1.0
    - Test 2: minFlingVelocity is 15.0
    - Test 3: maxFlingVelocity is 20000.0
    - Test 4: carriedMomentum with positive velocity returns clamped * 12
    - Test 5: carriedMomentum with negative velocity returns negative clamped * 12
    - Test 6: carriedMomentum with zero velocity returns 0
    - Test 7: spring has expected mass (0.3), stiffness (300.0), ratio (damping ratio 0.8)
    - Test 8: applyTo(null) returns SnappyPageScrollPhysics instance
  </behavior>
  <action>
    Create `test/constants/` directory (does not exist yet).

    For spacing_test.dart:
    - Import `package:flutter/material.dart` and `package:flutter_test/flutter_test.dart`
    - Import `package:xfin/constants/spacing.dart`
    - Use group('numeric constants') for the 5 double values
    - Use group('vertical SizedBox widgets') — cast each to SizedBox, verify height
    - Use group('horizontal SizedBox widgets') — cast each to SizedBox, verify width
    - Pattern: `expect((Spacing.vTiny as SizedBox).height, 4.0)`

    For snappy_scroll_physics_test.dart:
    - Import `package:flutter/material.dart` and `package:flutter_test/flutter_test.dart`
    - Import `package:xfin/utils/snappy_scroll_physics.dart`
    - Use group('SnappyPageScrollPhysics')
    - Create instance: `const physics = SnappyPageScrollPhysics()`
    - Test property getters directly
    - For carriedMomentum: test with 5000.0 (under clamp), 15000.0 (over clamp at 10000), -5000.0, 0.0
    - For spring: verify mass, stiffness. Check damping > 0.
    - For applyTo: `expect(physics.applyTo(null), isA<SnappyPageScrollPhysics>())`

    Follow the pattern from test/utils/format_test.dart: simple group/test with direct assertions.
  </action>
  <verify>
    <automated>flutter test test/constants/spacing_test.dart test/utils/snappy_scroll_physics_test.dart</automated>
  </verify>
  <done>
    - All 15 spacing constant tests pass, verifying exact numeric values and SizedBox dimensions
    - All 8 scroll physics tests pass, verifying overridden properties and carriedMomentum edge cases
    - 100% statement coverage on both source files
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widget test for modal_helper.dart and unit tests for database_provider.dart</name>
  <files>test/utils/modal_helper_test.dart, test/providers/database_provider_test.dart</files>
  <behavior>
    modal_helper_test.dart (~5 tests):
    - Test 1: showFormModal opens a modal bottom sheet
    - Test 2: showFormModal displays the passed formWidget inside the sheet
    - Test 3: showFormModal returns the result when sheet is dismissed with a value
    - Test 4: showFormModal returns null when sheet is dismissed without value
    - Test 5: showFormModal uses isScrollControlled: true (sheet takes full height)

    database_provider_test.dart (~7 tests):
    - Test 1: DatabaseProvider.instance returns same instance (singleton)
    - Test 2: initialize sets db property
    - Test 3: initialize calls notifyListeners
    - Test 4: replaceDatabase closes old database
    - Test 5: replaceDatabase sets new database
    - Test 6: replaceDatabase calls notifyListeners
    - Test 7: @visibleForTesting instance setter replaces singleton
  </behavior>
  <action>
    For modal_helper_test.dart:
    - Import flutter_test, material, and `package:xfin/utils/modal_helper.dart`
    - Use `testWidgets` since this is a widget test requiring BuildContext
    - Pump a MaterialApp with a Scaffold containing a Builder. Inside the builder, call showFormModal on button tap.
    - Pass a simple `Text('Test Form')` as formWidget
    - Verify the text appears in the bottom sheet after tapping trigger
    - To test return value: use `Navigator.pop(context, 'result')` from inside the form widget
    - To test isScrollControlled: verify the sheet can take near-full screen height (check BottomSheet presence and formWidget rendering)
    - Keep it simple — no need for database or providers, just MaterialApp + Scaffold + Builder

    For database_provider_test.dart:
    - Import flutter_test, `package:drift/native.dart`, `package:xfin/database/app_database.dart`, `package:xfin/providers/database_provider.dart`
    - Follow the pattern from test/providers/theme_provider_test.dart: group(), setUp(), tearDown()
    - In setUp: create a fresh DatabaseProvider using the @visibleForTesting setter: `DatabaseProvider.instance = DatabaseProvider._internal()` — BUT since _internal is private, use a workaround: save original instance, test with it, restore in tearDown
    - Actually: since DatabaseProvider has `static set instance(DatabaseProvider provider)` as @visibleForTesting, just reset to a fresh provider. But the constructor is private. Instead, use the existing singleton and call initialize() with in-memory db in setUp.
    - Correct approach: Use `DatabaseProvider.instance` directly (it's a singleton). In setUp, create in-memory AppDatabase, call `DatabaseProvider.instance.initialize(db)`. In tearDown, close db.
    - For notifyListeners: use `addListener(() => notified = true)` pattern (from base_currency_provider_test.dart)
    - For replaceDatabase: initialize with db1, then call replaceDatabase(db2), verify db property is db2

    Use Ralph Loop: implement -> flutter test -> fix if failing -> repeat until green.
  </action>
  <verify>
    <automated>flutter test test/utils/modal_helper_test.dart test/providers/database_provider_test.dart</automated>
  </verify>
  <done>
    - All 5 modal_helper tests pass, verifying sheet opens, widget renders, result returns, isScrollControlled
    - All 7 database_provider tests pass, verifying singleton, initialize, replaceDatabase, notifyListeners
    - 100% statement coverage on both source files
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite and analyzer:

1. `flutter test` — all project tests pass (currently ~922+, expect ~957+ after this plan)
2. `flutter analyze` — zero issues
3. Verify all 4 new test files exist:
   - test/constants/spacing_test.dart
   - test/utils/modal_helper_test.dart
   - test/utils/snappy_scroll_physics_test.dart
   - test/providers/database_provider_test.dart
</verification>

<success_criteria>
- 4 new test files created with ~35 total tests
- 100% statement coverage on all 4 source files (spacing.dart, modal_helper.dart, snappy_scroll_physics.dart, database_provider.dart)
- Full project test suite passes (flutter test)
- Zero flutter analyze issues
- Every utility, constant, and provider file in the project now has corresponding tests
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-utils-providers/05-01-SUMMARY.md`
</output>
