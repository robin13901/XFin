---
phase: 03-refactor-large-files
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/screens/calendar_screen.dart
  - lib/utils/snappy_scroll_physics.dart
  - lib/screens/calendar/calendar_data.dart
  - lib/screens/calendar/month_summary.dart
  - lib/screens/calendar/month_grid.dart
  - lib/screens/calendar/day_details.dart
autonomous: true
requirements: [R4.2]

must_haves:
  truths:
    - "Calendar screen renders and behaves identically -- month navigation, day taps, summary section all work"
    - "All existing calendar_screen_test.dart tests pass without modification"
    - "calendar_screen.dart is under 400 lines after extraction"
    - "SnappyPageScrollPhysics is reusable from utils/"
  artifacts:
    - path: "lib/utils/snappy_scroll_physics.dart"
      provides: "Public SnappyPageScrollPhysics class"
      contains: "class SnappyPageScrollPhysics extends PageScrollPhysics"
    - path: "lib/screens/calendar/calendar_data.dart"
      provides: "CalendarScreenData data class"
      contains: "class CalendarScreenData"
    - path: "lib/screens/calendar/month_summary.dart"
      provides: "MonthHeader, MonthSummarySection, CategoryListWrapper widgets"
      contains: "class MonthSummarySection"
    - path: "lib/screens/calendar/month_grid.dart"
      provides: "MonthGrid widget"
      contains: "class MonthGrid"
    - path: "lib/screens/calendar/day_details.dart"
      provides: "DayDetailsPager, DayDetailsPage, SimpleDetailRow"
      contains: "class DayDetailsPager"
  key_links:
    - from: "lib/screens/calendar_screen.dart"
      to: "lib/utils/snappy_scroll_physics.dart"
      via: "import for SnappyPageScrollPhysics"
      pattern: "import.*snappy_scroll_physics"
    - from: "lib/screens/calendar_screen.dart"
      to: "lib/screens/calendar/*.dart"
      via: "imports for extracted widgets and data class"
      pattern: "import.*calendar/"
    - from: "lib/screens/calendar/day_details.dart"
      to: "lib/utils/snappy_scroll_physics.dart"
      via: "DayDetailsPager uses SnappyPageScrollPhysics in its PageView"
      pattern: "SnappyPageScrollPhysics"
---

<objective>
Extract calendar_screen.dart (922 lines) into focused modules: utility scroll physics, data class, and 3 widget groups.

Purpose: The calendar screen file contains 12 classes mixing concerns (scroll physics, data models, display widgets, state management). Extracting display widgets and data classes into a `screens/calendar/` subdirectory brings the main file under 400 lines and gives each extracted file a clear single responsibility.

Output: 5 new files (1 in utils/, 4 in screens/calendar/), main calendar_screen.dart reduced to ~390 lines.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/3/03-RESEARCH.md
@lib/screens/calendar_screen.dart
@test/screens/calendar_screen_test.dart
</context>

<interfaces>
<!-- Classes to extract from calendar_screen.dart with their line ranges and dependencies -->

<!-- 1. _SnappyPageScrollPhysics (lines 28-55) -> SnappyPageScrollPhysics -->
<!--    Self-contained, extends PageScrollPhysics. Used at lines 394 and 885. -->
<!--    Only import needed: flutter/material.dart -->

<!-- 2. _CalendarScreenData (lines 617-627) -> CalendarScreenData -->
<!--    Used by: _CalendarScreenState (cache maps, FutureBuilder), _MonthSummarySection, _MonthGrid -->
```dart
class _CalendarScreenData {
  final DateTime month;
  final Map<int, double> dayNetFlow;
  final MonthlyAnalysisSnapshot monthlySnapshot;
}
```
<!--    Imports: MonthlyAnalysisSnapshot from analysis_dao.dart -->

<!-- 3. _MonthHeader (lines 445-461), _MonthSummarySection (lines 463-529), _CategoryListWrapper (lines 532-615) -->
<!--    All StatelessWidgets. _MonthSummarySection takes _CalendarScreenData as param. -->
<!--    _CategoryListWrapper uses chartColors from global_constants.dart and calculateCategoryData from category_widgets.dart -->

<!-- 4. _MonthGrid (lines 629-805) -- largest extracted widget (177 lines) -->
<!--    StatelessWidget taking month, dayNetFlow, onDayTap callback -->
<!--    Uses AppColors, formatCurrency, l10n calendar weekday strings -->

<!-- 5. _DayDetailsPager (lines 826-922), _DayDetailsPage (lines 807-812), _SimpleDetailRow (lines 814-824) -->
<!--    DayDetailsPager uses _SnappyPageScrollPhysics (line 885) and LiquidGlass widgets -->
<!--    _DayDetailsPage and _SimpleDetailRow are simple data classes used by _CalendarScreenState._buildDetailPages -->

<!-- The _CalendarScreenState methods _buildDetailPages and _compactList create _DayDetailsPage and _SimpleDetailRow -->
<!-- After extraction, these become DayDetailsPage and SimpleDetailRow (public) -->
<!-- _CalendarScreenState will import from calendar/day_details.dart -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract scroll physics, data class, and widget files</name>
  <files>
    lib/utils/snappy_scroll_physics.dart
    lib/screens/calendar/calendar_data.dart
    lib/screens/calendar/month_summary.dart
    lib/screens/calendar/month_grid.dart
    lib/screens/calendar/day_details.dart
  </files>
  <action>
Create 5 new files by extracting classes from `calendar_screen.dart`. All extracted private classes become public (remove `_` prefix).

**1. `lib/utils/snappy_scroll_physics.dart`** (~30 lines):
Extract `_SnappyPageScrollPhysics` (lines 28-55) as `SnappyPageScrollPhysics`.
- Change class name: `_SnappyPageScrollPhysics` -> `SnappyPageScrollPhysics`
- Change `applyTo` return type to match: `SnappyPageScrollPhysics`
- Single import: `package:flutter/material.dart`

**2. `lib/screens/calendar/calendar_data.dart`** (~15 lines):
Extract `_CalendarScreenData` (lines 617-627) as `CalendarScreenData`.
- Import: `package:xfin/database/daos/analysis_dao.dart` (for `MonthlyAnalysisSnapshot`)

**3. `lib/screens/calendar/month_summary.dart`** (~170 lines):
Extract 3 widgets:
- `_MonthHeader` (lines 445-461) -> `MonthHeader`
- `_MonthSummarySection` (lines 463-529) -> `MonthSummarySection`
- `_CategoryListWrapper` (lines 532-615) -> `CategoryListWrapper`

Change `_CalendarScreenData` references to `CalendarScreenData`.
Imports needed:
- `package:flutter/material.dart`
- `package:intl/intl.dart`
- `package:xfin/l10n/app_localizations.dart`
- `package:xfin/app_theme.dart` (for `AppColors`)
- `package:xfin/utils/format.dart` (for `formatCurrency`)
- `package:xfin/utils/global_constants.dart` (for `chartColors`)
- `package:xfin/widgets/category_widgets.dart` (for `calculateCategoryData`, `CategoryPieChart`)
- `package:xfin/widgets/common_widgets.dart` (for `SectionTitle`)
- `package:xfin/widgets/inflow_outflow_toggle.dart`
- `package:xfin/widgets/summary_row.dart`
- `calendar_data.dart`

**4. `lib/screens/calendar/month_grid.dart`** (~180 lines):
Extract `_MonthGrid` (lines 629-805) as `MonthGrid`.
Imports needed:
- `package:flutter/material.dart`
- `package:xfin/app_theme.dart` (for `AppColors`)
- `package:xfin/l10n/app_localizations.dart`
- `package:xfin/utils/format.dart` (for `formatCurrency`)

**5. `lib/screens/calendar/day_details.dart`** (~120 lines):
Extract 3 classes:
- `_DayDetailsPage` (lines 807-812) -> `DayDetailsPage`
- `_SimpleDetailRow` (lines 814-824) -> `SimpleDetailRow`
- `_DayDetailsPager` + `_DayDetailsPagerState` (lines 826-922) -> `DayDetailsPager` + `DayDetailsPagerState`

In `_DayDetailsPagerState.build()` at line 885, change `_SnappyPageScrollPhysics()` to `SnappyPageScrollPhysics()`.
Imports needed:
- `package:flutter/material.dart`
- `package:intl/intl.dart`
- `package:liquid_glass_renderer/liquid_glass_renderer.dart`
- `package:xfin/database/daos/analysis_dao.dart` (for `CalendarDayDetails`)
- `package:xfin/widgets/liquid_glass_widgets.dart` (for `liquidGlassSettings`)
- `package:xfin/utils/snappy_scroll_physics.dart`

**After creating all 5 files,** verify each compiles: `flutter analyze lib/utils/snappy_scroll_physics.dart lib/screens/calendar/`
  </action>
  <verify>
    <automated>cd c:/SAPDevelop/Privat/XFin && flutter analyze lib/utils/snappy_scroll_physics.dart lib/screens/calendar/</automated>
  </verify>
  <done>5 new files created. Each compiles cleanly. All private classes are now public.</done>
</task>

<task type="auto">
  <name>Task 2: Update calendar_screen.dart and verify full suite</name>
  <files>
    lib/screens/calendar_screen.dart
  </files>
  <action>
Modify `lib/screens/calendar_screen.dart` to import from the extracted files and remove the extracted class definitions.

**Add imports** (after existing imports):
```dart
import '../utils/snappy_scroll_physics.dart';
import 'calendar/calendar_data.dart';
import 'calendar/day_details.dart';
import 'calendar/month_grid.dart';
import 'calendar/month_summary.dart';
```

**Remove these class definitions** from the file:
- `_SnappyPageScrollPhysics` (lines 28-55)
- `_MonthHeader` (lines 445-461)
- `_MonthSummarySection` (lines 463-529)
- `_CategoryListWrapper` (lines 532-615)
- `_CalendarScreenData` (lines 617-627)
- `_MonthGrid` (lines 629-805)
- `_DayDetailsPage` (lines 807-812)
- `_SimpleDetailRow` (lines 814-824)
- `_DayDetailsPager` + `_DayDetailsPagerState` (lines 826-922)

**Rename all references** within `_CalendarScreenState` from private to public names:
- `_SnappyPageScrollPhysics` -> `SnappyPageScrollPhysics` (line 394)
- `_CalendarScreenData` -> `CalendarScreenData` (type annotations in cache maps, FutureBuilder, etc.)
- `_MonthHeader` -> `MonthHeader` (line 348)
- `_MonthSummarySection` -> `MonthSummarySection` (line 361)
- `_MonthGrid` -> `MonthGrid` (lines 404, 420)
- `_DayDetailsPage` -> `DayDetailsPage` (in `_buildDetailPages` method return type and all constructor calls)
- `_SimpleDetailRow` -> `SimpleDetailRow` (in `_compactList` parameter type and all constructor calls)
- `_DayDetailsPager` -> `DayDetailsPager` (line 185)

**Keep in calendar_screen.dart:**
- `CalendarScreen` (StatefulWidget entry point)
- `_CalendarScreenState` (with all state management, caching, data loading, `_openDayDetails`, `_buildDetailPages`, `_compactList`, `_statLine`, `build`, `_buildCalendarPager`, `_calendarPagerViewportHeight`)

The resulting file should be ~390 lines (the state class with its methods + the CalendarScreen widget).

**After modification,** run the full test suite and analyzer to verify zero regressions.
  </action>
  <verify>
    <automated>cd c:/SAPDevelop/Privat/XFin && flutter test && flutter analyze</automated>
  </verify>
  <done>calendar_screen.dart reduced to ~390 lines. All 512+ tests pass. Zero flutter analyze issues. Calendar screen behavior is identical.</done>
</task>

</tasks>

<verification>
1. `flutter analyze` returns zero issues
2. `flutter test` -- all 512+ tests pass
3. `flutter test test/screens/calendar_screen_test.dart` -- all calendar tests pass without modification
4. `calendar_screen.dart` is under 400 lines
5. No extracted file exceeds 200 lines
6. `SnappyPageScrollPhysics` is importable from `lib/utils/snappy_scroll_physics.dart`
</verification>

<success_criteria>
- calendar_screen.dart reduced from 922 to ~390 lines
- 5 extracted files each have clear single responsibility
- All existing calendar tests pass without modification
- SnappyPageScrollPhysics is a reusable public utility class
- No circular imports between calendar_screen.dart and extracted files
- Zero flutter analyze issues
</success_criteria>

<output>
After completion, create `.planning/phases/3/03-02-SUMMARY.md`
</output>
